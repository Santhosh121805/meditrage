#!/usr/bin/env python
"""Setup configuration for MedTriage package."""

from setuptools import setup, find_packages

setup(
    name="medtriage",
    version="1.0.0",
    packages=find_packages(),
    include_package_data=True,
)
